import 'package:flutter/material.dart';

class AppColors {
  // Instagram Gradient
  static const igPurple = Color(0xFF833AB4);
  static const igPink = Color(0xFFFD1D1D);
  static const igOrange = Color(0xFFFCAF45);

  // Actions
  static const downloadGreen = Color(0xFF4CAF50);
  static const accentBlue = Color(0xFF2979FF);
  static const progressPink = Color(0xFFFF2E63);

  // Light Mode
  static const lightBackground = Color(0xFFFFFFFF);
  static const lightSurface = Color(0xFFF5F5F7);
  static const lightTextPrimary = Color(0xFF111111);
  static const lightTextSecondary = Color(0xFF666666);

  // Dark Mode
  static const darkBackground = Color(0xFF0F0F14);
  static const darkSurface = Color(0xFF1C1C24);
  static const darkTextPrimary = Color(0xFFFFFFFF);
  static const darkTextSecondary = Color(0xFFB3B3B3);

  // Dark Mode Cards
  static const darkCardBackground = Color(0xFF232733);
  static const darkCardBorder = Color(0xFF2E3444);
  static const darkCardTitle = Color(0xFFEDEFF2);
  static const darkCardSubtitle = Color(0xFFA1A6B3);

  // Light Mode Cards
  static const lightCardBackground = Color(0xFFEFF1F5);
  static const lightCardBorder = Color(0xFFD8DCE4);
  static const lightCardTitle = Color(0xFF1A1C20);
  static const lightCardSubtitle = Color(0xFF4B5563);

  // Common
  static const shareIcon = Color(0xFFF28C28);
}
